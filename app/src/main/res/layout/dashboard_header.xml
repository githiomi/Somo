<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/black"
    android:padding="5dp">

    <ImageView
        android:id="@+id/ivOpenDrawer"
        android:layout_width="45dp"
        android:layout_height="45dp"
        android:layout_centerVertical="true"
        android:clickable="true"
        android:contentDescription="@string/app_name"
        android:padding="3dp"
        android:src="@drawable/ic_menu" />

    <TextView
        android:id="@+id/userCredentials"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_toStartOf="@id/userProfilePicture"
        android:layout_toEndOf="@id/ivOpenDrawer"
        android:padding="3dp"
        android:text="@string/dashboard_header_placeholder"
        android:textAlignment="center"
        android:textColor="@color/vote_logo_color"
        android:textSize="20sp"
        android:textStyle="bold" />

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/userProfilePicture"
        android:layout_width="45dp"
        android:layout_height="45dp"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:clickable="true"
        android:padding="3dp"
        android:scaleType="centerCrop"
        android:src="@drawable/profile_picture_placeholder" />

</RelativeLayout>